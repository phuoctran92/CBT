import { createStyles, makeStyles } from "@material-ui/core/styles";

export default makeStyles(() =>
  createStyles({
    container: {
      width: '100%',
      background: '#FFF',
      marginTop: 10,
      padding: 20,
      borderRadius: 5
    },
  }))